# API Evidence — PLACEHOLDER

S1 has no API/swagger source registered for this project yet, so API evidence
(write contracts, recipes, orchestration endpoints) is **not** captured here.

**Open question:** register the domain backends' swagger/API snapshots and the
permission registries as S1 inputs, then extend `builder/stages/s1-evidence.mjs`
to pin them into `apis/` (and permission evidence) alongside tables/ and views/.

Until then, S4 write-contract derivation must treat API evidence as a
PLACEHOLDER per constitution §2.1 (evidence-or-placeholder), never a guess.
